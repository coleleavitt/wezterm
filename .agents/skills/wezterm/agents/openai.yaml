interface:
  display_name: "Wezterm"
  short_description: "Repo-specific patterns and workflows for wezterm"
  default_prompt: "Use the wezterm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true